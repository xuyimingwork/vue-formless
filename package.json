{
  "name": "vue-formless-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter vue-formless build",
    "test": "pnpm --filter vue-formless test",
    "test:watch": "pnpm --filter vue-formless test:watch",
    "typecheck": "pnpm --filter vue-formless typecheck && pnpm --filter playground typecheck",
    "playground": "pnpm --filter playground dev"
  },
  "engines": {
    "node": ">=18"
  }
}
